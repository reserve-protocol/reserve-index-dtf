[profile.default]
# Directory Setup
script = "script"
src = "contracts"
test = "test"

# Tests
gas_limit = 1000000000000 # 1t gas
memory_limit = 16000000000 # 16gb

# Compiler Options
evm_version = "paris"
optimizer = true
optimizer_runs = 200
solc_version = "0.8.28"

# Permissions
fs_permissions = [{access = "read-write", path = "./"}]

additional_compiler_profiles = [
  {name = "test", via_ir = false},
]

compilation_restrictions = [
  {paths = "test/**", via_ir = false},
]
